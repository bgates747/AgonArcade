# September 11, 2026 — straight-road prototype

1. RALLY-01 scope narrowed by the user to a straight road, gray surface,
   red/white kerbs, yellow dotted/dashed center marking, acceleration and braking.
   Steering and the broader research-plan features remain deferred.
2. Added native C++17 `rally/` sibling project, isolated generated emulator
   profile, build, host checks, launcher and handoff. Starts stationary; Up adds
   speed, Down brakes, release coasts, Escape returns to MOS.
3. Uses mode 136, complete opaque composition, ordinary filled triangles and
   no software sprites. Projection depth table drives world-attached material
   phases. Consecutive equal rows merge; road/background stream peaks at 828
   bytes across all 8,000 hundredth-unit phases. HUD/swap cost is additional.
   Scheduling target is 25 fps; physical hardware throughput is not measured.
4. Native compilation and sanitizer host checks pass. First headless capture
   exposed an uninitialized depth table with global-constructor initialization;
   explicit initialization in main fixed the native rendering. Verified packed
   triangle fills visually in the corrected capture.
5. Headless SDL input sequence accelerated to 300, coasted, braked to zero and
   escaped cleanly to MOS. Coasting road captures differ; stopped captures at
   presentation frames 720 and 780 are pixel-identical. Capture helper is local
   and ignored; representative image is `rally/docs/straight-road.png`.
6. Initial capture-helper compilation omitted the local SDL include directory;
   that attempt timed out without captures. Corrected helper compilation and
   repeated the complete capture successfully, exit zero.
7. Prepared graphical Rally review profile. Human gameplay validation and
   explicit commit approval are pending under RALLY-01; nothing committed/pushed.

8. RALLY-03 direct-GPIO VGA secondary target shelved at user request. Removed
   from the active TODO; feasibility notes remain in
   `research/rally-gpio-vga.md`. No adapter implementation or hardware change
   was made. Resume only on a new user request.

9. User requested a lower viewpoint and no visible vanishing point, supplying
   an original-game reference. Halved camera height to 50 with the horizon at
   row 96; truncated visible ground at row 103 (finite road width 24 pixels).
   Foreground half-width is now 228 pixels and clips at the screen sides.
   Projection depth and material mapping use the same new height. Host checks
   pass across all phases, with a new maximum of 504 road/background bytes.
   Headless keyboard capture completed successfully; visually confirmed wide
   foreground clipping and finite skyline cutoff. Refreshed the README capture
   and relaunched the graphical review profile. Human visual tuning pending.

10. User accepted the lower viewpoint and authorized curves using the supplied
    arcade track map. Inspected AgonJukebox's ignored SKIN-013 Potrace bridge and
    tracing notes. Its region-outline workflow was not needed for the small,
    readable centreline; manually digitized cubic segments in reference-image
    coordinates, retaining the top-straight start and clockwise direction.
11. Added reproducible SVG/CSV/C++ generation, 512 equally arc-spaced samples,
    continuous position/tangent interpolation and automatic track following.
    Preserved accepted camera calibration and speed controls. Curves project
    actual traced lateral displacement with arc distance as depth, explicitly
    an arcade approximation. Track length is 32,768 arbitrary units; no claim
    to original ROM geometry or the map's printed physical lap length.
12. Added material-preserving adaptive bands. Host loop sweep (17-unit spacing)
    observed 1,125 road/background bytes and 17 bands maximum. ASan/UBSan checks
    include interpolation boundaries, lap seam, motion wrap and one-pixel band
    centreline error before integer-coordinate rounding. All pass.
13. Native right-turn capture revealed horizontal gaps despite clean host raster
    previews. Sharing the next band's boundary row resolved the VDP-specific
    triangle coverage issue. Repeated right/left native input captures passed;
    checked all inspected interior road rows have asphalt and braked frames are
    identical. Escape returns to MOS. Saved representative native screenshots.
14. Relaunched graphical review immediately before the first right bend using
    `run . 4800`. Normal generated startup remains `run` at the map start; the
    current local review autoexec deliberately selects the bend. Human validation
    and explicit commit approval remain pending; nothing committed or pushed.

15. User accepted the curved-track result, then named a Stunt Car Racer-inspired
    custom-VDP true-3D project as the next direction. Recorded STUNT-01 and a
    brief with inspected Pingo examples. No new game or firmware implementation
    has started, and no changes were committed.

16. At user request, built the current curve prototype and deployed its
    16,521-byte standalone binary to the mounted AGON card at
    `/mystuff/arcade/rally/rally.bin`. Added a narrowly scoped deployment helper
    that validates the mounted volume and atomically replaces only the binary.
    Flushed writes; left autoexec.txt untouched. Physical gameplay validation
    remains separate from the successful file deployment.

17. RALLY-01 complete: user reports "hardware test passes" for the deployed
    flat-track prototype, following prior emulator acceptance. Removed validation
    from the active TODO. This is user-reported hardware acceptance, not a measured
    hardware frame-rate result. Commit/push approval has not been given.

18. User explicitly authorized the local milestone commit after the hardware
    pass, with no remote work until tomorrow. Included Rally source, generated
    track data, research, screenshots, deployment tools and project handoffs.
    Ignored emulator state, build products and the research source cache remain
    outside Git. No push or remote configuration change is authorized this turn.
