# September 11, 2026 — straight-road prototype

1. RALLY-01 scope narrowed by the user to a straight road, gray surface,
   red/white kerbs, yellow dotted/dashed center marking, acceleration and braking.
   Steering and the broader research-plan features remain deferred.
2. Added native C++17 `rally/` sibling project, isolated generated emulator
   profile, build, host checks, launcher and handoff. Starts stationary; Up adds
   speed, Down brakes, release coasts, Escape returns to MOS.
3. Uses mode 136, complete opaque composition, ordinary filled triangles and
   no software sprites. Projection depth table drives world-attached material
   phases. Consecutive equal rows merge; road/background stream peaks at 828
   bytes across all 8,000 hundredth-unit phases. HUD/swap cost is additional.
   Scheduling target is 25 fps; physical hardware throughput is not measured.
4. Native compilation and sanitizer host checks pass. First headless capture
   exposed an uninitialized depth table with global-constructor initialization;
   explicit initialization in main fixed the native rendering. Verified packed
   triangle fills visually in the corrected capture.
5. Headless SDL input sequence accelerated to 300, coasted, braked to zero and
   escaped cleanly to MOS. Coasting road captures differ; stopped captures at
   presentation frames 720 and 780 are pixel-identical. Capture helper is local
   and ignored; representative image is `rally/docs/straight-road.png`.
6. Initial capture-helper compilation omitted the local SDL include directory;
   that attempt timed out without captures. Corrected helper compilation and
   repeated the complete capture successfully, exit zero.
7. Prepared graphical Rally review profile. Human gameplay validation and
   explicit commit approval are pending under RALLY-01; nothing committed/pushed.

8. RALLY-03 direct-GPIO VGA secondary target shelved at user request. Removed
   from the active TODO; feasibility notes remain in
   `research/rally-gpio-vga.md`. No adapter implementation or hardware change
   was made. Resume only on a new user request.

9. User requested a lower viewpoint and no visible vanishing point, supplying
   an original-game reference. Halved camera height to 50 with the horizon at
   row 96; truncated visible ground at row 103 (finite road width 24 pixels).
   Foreground half-width is now 228 pixels and clips at the screen sides.
   Projection depth and material mapping use the same new height. Host checks
   pass across all phases, with a new maximum of 504 road/background bytes.
   Headless keyboard capture completed successfully; visually confirmed wide
   foreground clipping and finite skyline cutoff. Refreshed the README capture
   and relaunched the graphical review profile. Human visual tuning pending.

10. User accepted the lower viewpoint and authorized curves using the supplied
    arcade track map. Inspected AgonJukebox's ignored SKIN-013 Potrace bridge and
    tracing notes. Its region-outline workflow was not needed for the small,
    readable centreline; manually digitized cubic segments in reference-image
    coordinates, retaining the top-straight start and clockwise direction.
11. Added reproducible SVG/CSV/C++ generation, 512 equally arc-spaced samples,
    continuous position/tangent interpolation and automatic track following.
    Preserved accepted camera calibration and speed controls. Curves project
    actual traced lateral displacement with arc distance as depth, explicitly
    an arcade approximation. Track length is 32,768 arbitrary units; no claim
    to original ROM geometry or the map's printed physical lap length.
12. Added material-preserving adaptive bands. Host loop sweep (17-unit spacing)
    observed 1,125 road/background bytes and 17 bands maximum. ASan/UBSan checks
    include interpolation boundaries, lap seam, motion wrap and one-pixel band
    centreline error before integer-coordinate rounding. All pass.
13. Native right-turn capture revealed horizontal gaps despite clean host raster
    previews. Sharing the next band's boundary row resolved the VDP-specific
    triangle coverage issue. Repeated right/left native input captures passed;
    checked all inspected interior road rows have asphalt and braked frames are
    identical. Escape returns to MOS. Saved representative native screenshots.
14. Relaunched graphical review immediately before the first right bend using
    `run . 4800`. Normal generated startup remains `run` at the map start; the
    current local review autoexec deliberately selects the bend. Human validation
    and explicit commit approval remain pending; nothing committed or pushed.

15. User accepted the curved-track result, then named a Stunt Car Racer-inspired
    custom-VDP true-3D project as the next direction. Recorded STUNT-01 and a
    brief with inspected Pingo examples. No new game or firmware implementation
    has started, and no changes were committed.

16. At user request, built the current curve prototype and deployed its
    16,521-byte standalone binary to the mounted AGON card at
    `/mystuff/arcade/rally/rally.bin`. Added a narrowly scoped deployment helper
    that validates the mounted volume and atomically replaces only the binary.
    Flushed writes; left autoexec.txt untouched. Physical gameplay validation
    remains separate from the successful file deployment.

17. RALLY-01 complete: user reports "hardware test passes" for the deployed
    flat-track prototype, following prior emulator acceptance. Removed validation
    from the active TODO. This is user-reported hardware acceptance, not a measured
    hardware frame-rate result. Commit/push approval has not been given.

18. User explicitly authorized the local milestone commit after the hardware
    pass, with no remote work until tomorrow. Included Rally source, generated
    track data, research, screenshots, deployment tools and project handoffs.
    Ignored emulator state, build products and the research source cache remain
    outside Git. No push or remote configuration change is authorized this turn.

19. RALLY-04: user requested the player-car model after agreeing on pre-rendered
    yaw views and a restricted palette. Built an original open-wheel racer in
    Blender 4.0.2, with named meshes and an editable yaw root. Flat materials,
    orthographic camera, no shadows/specular/antialiasing. Saved the .blend and
    reproducible builder; no generative image transformer used.
20. Exported nine 64x48 views spanning -30..30 degrees in 7.5-degree steps.
    Ten opaque palette colors, binary alpha, 27,648 packed RGBA2222 bytes total.
    Verified all packed pixels round-trip exactly to PNG and every view fits
    inside its canvas. Added angle sheet and explicitly labeled static road
    mockup. Model review pending; no game integration or steering changes.

21. User authorized driving after model inspection. Embedded all nine car views
    in the native binary and preloaded them as ordinary VDP bitmaps. Added
    Left/Right steering with rapid yaw response and hysteresis, smoothed lateral
    velocity, partial lateral camera following, outward curve slip and grass
    slowdown. Up/Down retain acceleration/braking. No software sprites used.
22. Native binary is 45,829 bytes and remains self-contained. Sanitizer checks
    pass for steering symmetry/sign, yaw return, grass slowdown, no stopped-car
    translation, curve-slip sign, camera parallax and offset render bounds.
    Scripted native inputs visibly turn both ways, enter grass, slow, stop and
    exit to MOS; stopped captures are identical. Saved driving screenshot.
23. Relaunched the local Rally profile at the normal start for human driving
    review. No SD redeployment, commit or push was performed. The earlier
    road-only hardware acceptance does not qualify this new driving build.

24. User accepted the driving feel and requested roughly double car size. Added
    exact 2x startup bitmap expansion (128x96) with adjusted center/ground anchor.
    Palette and embedded source assets are unchanged; VDP storage is now 110,592
    bytes, with a 12,288-byte reusable CPU upload scratch buffer. Native build
    passes; repeated headless driving capture for visual scale review.

25. User requested a 20% reduction from the doubled car. Changed display
    bitmaps from 128x96 to 102x77, retaining palette-safe nearest-neighbor
    sampling and adjusting the draw origin to preserve center/ground contact.
    Native build passed; captured the revised size for visual verification.

26. User accepted 102x77 car size and requested incremental steering. Each new
    physical Left/Right key-down changes one detent (-4..+4), release holds it,
    and held keys do not repeat. Opposite presses countersteer through zero.
    Simultaneous opposing keys make no change. HUD now shows selected detent.
    Build and sanitizer tests pass for persistence, held-key suppression,
    opposing presses, saturation, yaw direction and prior driving behavior.

27. Native short-tap test exposed a missed press with held-map polling. Switched
    steering to agondev keyboard events (64-event queue) and persistent key
    states, retaining edge suppression for auto-repeat. Kept throttle/brake
    held-map controls. Deinitialize the MOS keyboard handler before exit.

28. User clarified held-key incremental steering: snapshot all held keys each
    frame, apply steering once per frame, independent of built-in repeat. Removed
    the keyboard event queue and edge gating. Held Left/Right advances one detent
    per rendered frame; release retains angle. Physics catch-up is separate and
    cannot advance steering. Native build and host regression checks pass.

29. Deployed the current 45,749-byte driving binary to the AGON card at
    `/mystuff/arcade/rally/rally.bin`, leaving autoexec.txt untouched and flushing
    writes. The first attempt preceded the card mount; after mount appeared,
    the scoped deployment helper succeeded. No hardware pass for this revision
    has yet been reported.
30. User explicitly approved committing the driving milestone locally. Includes
    editable Blender car, palette-safe yaw assets and generators, embedded
    bitmap data, accepted 102x77 display scale, held-key frame-based steering,
    lateral driving behavior, tests and notes. Remote publication remains
    deferred; no push or remote changes.

31. RALLY-05: user requested a compact flat tri-oval with a locally tight turn,
    explicitly preserving Fuji. Moved editable cubics into named JSON profiles,
    embedded both tracks and added MOS selection (`run . fuji`, `run . oval`).
    Default tri-oval is 16,384 units versus Fuji's 32,768, counterclockwise and
    unbanked. Sampled minimum radii: 223.47 and 189.59 units respectively. Fuji
    geometry CSV compares byte-identical to the prior committed original.
32. Both track definitions pass native build and sanitizer host checks. Tri-oval
    sampled whole-loop/offset sweep peaks at 1,071 road bytes; sample seams and
    lap wrap pass. Native captures exercise both named track selections.
33. User additionally requested live -= grip tuning. Added held-key frame-based
    five-point adjustments from 25 to 200 percent, default 100, shown on HUD.
    Replaced the arbitrary outward-velocity term with a bounded lateral-force
    budget shared by steering demand and v-squared/radius curve demand. Excess
    demand causes track-relative slip. Grass halves the budget. Units remain
    uncalibrated arcade units. Host grip-force/bounds tests pass.

34. User requested temporary removal of the off-road speed penalty for grip
    testing. Added explicit `OffroadSpeedPenalty=false` tuning switch; throttle
    and brake still work and grass retains its reduced lateral grip. Host check
    verifies off-road coasting holds 200 units/second. This completes the combined
    tri-oval/selectable-Fuji/live-grip test build; human review remains pending.

35. User refined the drawing to bilateral symmetry, matching tighter end turns,
    a shallow front bend and a back straight one-third of the initial length.
    Removed extra annotations and kept the existing asset filenames. Start/finish
    moved to the center of the front bend. User approved the drawing and requested
    uniform scaling to twice Fuji's tightest radius before authorizing driving.
36. Applied the approved cubics to runtime. Desired lap 5,753.59 rounded to 5,760
    (0.11%) to retain 64-unit spacing; supports a non-power-of-two 90-point table.
    Analytic radius approximately 366.77 versus Fuji 183.18; sampled minima
    369.08 versus 189.59. Fuji CSV remains byte-identical to the committed original.
    Native build and both-track sanitizer checks pass, including lap seams.
    Grip controls and disabled off-road speed penalty remain in effect.

37. RALLY-06: steering now uses signed 16-bit angle units, 256 per circle,
    clamped ±21 (±29.53125 degrees). Each held rendered frame changes exactly
    one unit; lateral demand uses this fine angle with previous full-lock demand
    preserved. Artwork selection is separate, nearest of four magnitudes plus
    straight. Regenerated five source yaw views at 7.3828125-degree spacing.
38. User requested VDP matrix mirroring and preparation for future scaling.
    Five uploaded images supply nine displayed orientations using a draw-time
    affine X reflection and canvas-width compensation. Retained accepted 102×77
    startup enlargement. Embedded pixels reduced 27,648 to 15,360 bytes; VDP
    pixels 70,686 to 39,270. Matrix API notes recorded in research document.
39. Native build and ASan/UBSan suite pass: angle clamps, per-frame increments,
    release retention, physics independence, mirrored bin symmetry and fine
    lateral movement. Native captures show intermediate right steering, return
    through center, and left lock −21 with visible correctly facing artwork.
    Graphical profile reopened for user driving review; changes uncommitted.

40. User requested two angle ticks per rendered frame. Steering now adds ±2
    once per held-key snapshot and clamps at ±21; views and full-lock demand
    remain unchanged. Center to lock takes 11 frames, lock to lock 21.
    Native build and sanitizer checks pass, including endpoint clamping.

41. User found 130% grip sufficient to stay on the tri-oval at full throttle
    and selected it as the initial tuning baseline, accounting for Fuji’s
    tighter hairpin. Default is now 130%; live -= adjustment remains 25–200%
    in five-point increments. Native build and host sanitizer checks pass.

42. RALLY-07: user requested RAM pixel substitution before VDP upload for
    three opponent liveries, then specified yellow instead of magenta and
    British Racing Green. Replace only packed C2/C3/C7 body shades; blue,
    RGB222 dark-green approximation, yellow. Original five-view artwork is
    shared; tyres, helmet, wings, stripe and transparency remain unchanged.
43. Added three steady-line opponents at 150/170/190 units/sec. Arc-distance
    road projection, relative-tangent yaw selection, far-to-near ordering,
    and VDP Q8 scale/reflection matrices provide the first traffic pass.
    No collision response or overtaking AI. Twenty uploaded bitmaps occupy
    157,080 pixel bytes; larger upload extends startup.
44. Build and sanitizer checks pass, including all 256 possible input bytes
    for each livery (alpha and non-body preservation), plus traffic lap wrap.
    Native scripted capture shows all three colours and approaching scaled
    traffic. Graphical emulator reopened for human review; no commit/deploy.

45. User observed backwards-motion strobing above 224 and requested that as
    the player speed cap. Reduced cap from 300 to 224; opponent speeds remain
    150/170/190. Native build and host sanitizer speed/coasting checks pass.

46. With player speed now capped at 224, user requested default grip return
    to 100%. Live -= adjustment remains available. Native build and sanitizer
    checks pass.

47. User selected 60% default grip and requested three more opponents: cyan,
    light purple and orange, with complementary accents and varied helmets.
    Six traffic liveries now remap body shades, stripe, wings and helmet in
    RAM. Tyres, metal, visor and transparency remain intact. Lavender base
    RGB(170,85,255) distinguishes it from blue; BRG remains dark green.
48. Expanded traffic arrays, depth sorting and uploads to six opponents, with
    three fixed lanes and 140/160/180 speeds. Traffic matrix buffers moved to
    63970–63975 to avoid overlapping player reflection 63984. Thirty-five
    uploaded bitmaps use 274,890 pixel bytes, still five embedded source views.
49. Native build and sanitizer checks pass, including all-byte material/alpha
    preservation, distinct body colours and matrix-ID separation. Native capture
    shows expanded multicoloured traffic and 60% grip at speed 224. Graphical
    emulator reopened; no commit or hardware deployment.

50. User authorized VDP-side livery generation. Startup uploads only five
    original 102×77 images, then six 256-byte colour lookup tables. Buffered
    command 72 (options 0x10) derives thirty destination buffers from original
    sources, followed by explicit bitmap registration. Mapping scratch 63960
    is reused and freed; source pixels are never cumulatively recoloured.
51. Image/table UART payload is 40,806 bytes versus 274,890 image bytes before
    (85.2% reduction, command headers excluded). Final bitmap pixel RAM remains
    274,890 bytes. Native build and sanitizer palette/game checks pass. Native
    emulator captures show all six coloured opponents, scaled and mirrored,
    with unchanged player artwork. Graphical emulator reopened for review.

52. User requested three steering ticks per rendered frame, retaining ±21
    limits. Center to lock now takes seven frames; lock to lock fourteen.
    Fractional accumulation remains a possible follow-up, not implemented.
    Native build and sanitizer steering checks pass.

53. User accepted three-tick steering for now and explicitly requested a local
    commit with progress notes. RALLY-05/06/07 emulator review is accepted from
    iterative driving, tuning and this milestone approval; removed completed
    review tasks. RALLY-04 retains deployment/hardware validation, as the SD
    card still has the earlier driving binary. No hardware pass is inferred.
54. Milestone: compact symmetric 5,760-unit tri-oval with preserved Fuji;
    60% adjustable grip, 224 speed cap, no off-road speed penalty; signed
    256-circle steering at three ticks/frame; five source car views mirrored
    by the VDP; six coloured fixed-line opponents with complementary accents
    and helmets, depth scaling and VDP-side livery expansion. Upload payload
    reduced to 40,806 bytes. Native build, sanitizer suite and scripted native
    captures pass. Collisions/overtaking AI remain absent. Remote work deferred.

55. RALLY-08: user supplied scenic reference and requested programmatic sky
    gradients with Bayer dithering, clouds, foothills/mountain at infinity,
    wider kerbs and white/yellow shoulder lines. Installed local agon-utils
    1.1.0 into project .venv and used its palette Bayer converter. MoonPatrol
    could not be located; user suspects it never reached a repo and is searching
    cloud storage. This first panorama is independently procedural.
56. Added deterministic 1024×103 panorama: gradient, clouds, angular snow-capped
    peak and periodic foothills. Sixteen-colour 4-bit data uploads once (52,736
    bytes) and expands on VDP. Absolute track-tangent bearing uses integer atan
    lookup and one/two wrapped bitmap draws; all scenery shares one scroll.
    No lateral parallax or steering-linked fake heading.
57. Doubled kerb width from eight to sixteen world units per side; added inset
    white/yellow shoulders with two overdraw quads per band. Host full-loop
    sample peaks at 2,043 road bytes/17 bands; tri-oval offset sweep 1,908.
    Native build and sanitizer suite pass, including heading cardinal/octant
    checks and whole-track heading continuity. Native captures show scenery
    scrolling around bends and continuous widened kerbs/shoulders. Graphical
    emulator reopened for review. Changes uncommitted; no hardware deployment.

58. User found whole-panorama dithering excessive. Limited sky conversion to
    two blues, RGB(0,85,255) and RGB(85,85,255), then composited solid-colour
    clouds, mountain and foothills afterwards. Generator asserts the sky uses
    only those two colours. Regenerated assets visually inspected; build passes.

59. User requested the original gradient with slight deterministic noise and
    Floyd–Steinberg conversion. Restored original RGB gradient, added shared-
    channel ±4/255 noise with fixed seed 7472026, and switched sky-only agon-utils
    conversion to floyd. Solid scenery remains undithered. Generated panorama
    visually inspected; native build passes; emulator reopened for review.

60. User rejected sky dithering and requested flat shading with visible bands.
    Removed noise and switched sky conversion to nearest RGB palette colours.
    Regenerated panorama visually inspected; native build passes. Emulator
    reopened with solid sky bands and unchanged clouds/terrain/scrolling.

61. User selected solid cyan sky. Set RGB(0,255,255), removing the remaining
    gradient bands. Native build passes; emulator reopened for review.

62. User found pure cyan excessive and requested the closest palette colour
    to the latest reference’s upper sky band. Set solid RGB(0,85,255), keeping
    the sky free of gradients and dithering. Native build passes.

63. RALLY-09: user requested shoulder lines one stripe-width inward, kerb
    slowdown and restored grass slowdown. Lines moved from 86–88 to 84–86
    world units. Shared asphalt/kerb extents 90/106 and fixed car half-width
    12 classify ROAD/KERB/GRASS symmetrically. Kerbs drag speed toward 160
    (three units/centisecond); grass toward 90 (four). HUD shows surface.
64. User additionally requested a little extra kerb grip: applied 1.15 times
    configured grip on kerbs; grass retains 0.5 times. This is an arcade tuning
    choice. Build and sanitizer tests pass, including contact boundaries, drag
    ordering, full-throttle settling, braking to zero and relative grip budgets.
    Native surface capture confirms grass slowdown/HUD; updated kerb-grip
    build reopened for user review. Changes remain uncommitted.

65. User requested all-black tyres for asphalt contrast. Set tyre tread/facets
    and wheel hubs to black in the Blender source generator; retained other
    metal colours. Regenerated editable model, all five views and embedded
    data. Visual sprite-sheet inspection and native build pass. All seven
    liveries inherit the change. Emulator reopened.

66. User clarified hubs should be lighter than asphalt. Restored hub material
    to RGB(170,170,170), keeping tread/facets black. Regenerated model, views
    and binary; sprite-sheet inspection confirms visible light-grey hubs.
    Native build passes; emulator reopened.

67. User requested hardware test deployment with AGON inserted. Canonical
    scoped deployment helper wrote the current 96,333-byte rally.bin to
    /mystuff/arcade/rally/rally.bin and flushed writes. autoexec.txt untouched.
    Includes scenery, surface penalties/grip and black tyres/light-grey hubs.
    Hardware result pending.

68. Hardware test of deployed 96,333-byte build reports lag and backwards-motion
    strobing even at the reduced 224 speed cap. User also reports diminished
    smoothness in emulator, less severe than hardware. This is a performance
    regression, not a hardware pass. User explicitly authorized committing all
    current progress with this limitation before timing research.
69. Research authorized: trace Pingo custom firmware frame-completion callback
    through MOS keyboard-map machinery and fsim physics/render overlap timing.
    Produce durable notes and alert via graphical emulator when ready; prefer
    avoiding asynchronous complexity if simpler corrections suffice.

70. RALLY-10 research recorded in docs/research/rally-frame-pacing.md. Found
    MOS raw counter increments two per VBlank (120/s at 60 Hz), contrary to
    Rally’s nominal centisecond assumption; submission is not completion.
    Traced stock swap queue wait and proposed post-swap GP fence qualification.
    Identified broad shoulder overdraw and per-pixel affine work as profiling
    candidates. Pingo P3DR marks target-bitmap readiness, not screen display;
    fsim donors demonstrate bounded catch-up and one in-flight render. Modern
    fsim scheduler/profiling tasks are plans, not completed evidence. No game
    code or firmware changed during research. Emulator launched only as alert.

71. User clarified stock VDP is mandatory for shipping; custom callbacks are
    diagnostic only. Authorized first remediation: replace broad shoulder
    overdraw with two narrow stripes at the same coordinates/colours. Build
    and sanitizer checks pass; command count unchanged. Research document
    records attempt 1, expected raster benefit and pending hardware retest.

72. User authorized hardware deployment of remediation attempt 1. Scoped
    helper deployed 96,344-byte rally.bin to AGON:/mystuff/arcade/rally/rally.bin
    and flushed writes. autoexec.txt untouched; hardware retest result pending.

73. User authorized stock scenery scrolling as next remediation, requested a
    formal TODO and contract commit before implementation. RALLY-11 contract
    freezes independent draw-buffer history, clipped edge repaint, stock-only
    API and validation limits. Implementation follows this documentation commit.

74. RALLY-11 implemented independent retained offsets for two draw buffers,
    stock graphics-viewport scrolling and exposed-edge repaint. Build and
    sanitizer tests pass, including 1,500 alternating-buffer pixel simulations.
    Native comparison exposed foreground overlap at the horizon; contract
    amendment adds a bottom-16-row repair each frame to prevent stale traffic
    or road pixels. Upper sky remains retained. Hardware benefit unmeasured.

75. Native repaired-scroll validation: 23 captures exactly match an ideal
    panorama crop through row 101; row 102 remains the foreground boundary.
    Deployed 97,244-byte candidate to AGON:/mystuff/arcade/rally/rally.bin
    with autoexec.txt untouched immediately before launching the review emulator.
    Hardware performance result pending; implementation remains uncommitted.

76. User hardware review of retained scrolling: improvement is hard to judge.
    Authorized freezing as potential progress. Commit retains remediation 1
    (narrow shoulders) and remediation 2 (retained scenery with overlap repair),
    not a confirmed performance fix. Hardware gain remains inconclusive;
    RALLY-10 performance investigation and acceptance remain open.

77. RALLY-12: user requested indefinite centre-seeking demo mode. Added `demo`
    launch argument alongside either track/distance. Normal physics remains
    authoritative: damped lateral correction, bounded three-unit steering
    changes, and 512-unit bend look-ahead speed control using grip headroom.
    Driving arrows hand back to manual; -= still tunes grip; Escape exits.
78. Native build and sanitizer suite pass, including two laps of each track
    at 4/12/24-unit control intervals and recovery from a 40-unit offset. Native
    capture shows DEMO mode driving with traffic. Review profile autoexec selects
    demo; graphical emulator reopened. No SD deployment or commit.

79. Demo now defaults on launch; PRESS ANY KEY TO RACE replaces the HUD within
    the already repainted information band. Held launch keys are ignored until
    release; any new key takes over, with Escape consumed until release.
    `race` explicitly bypasses demo. A curvature-based demo steering baseline
    rotates the car art while residual steering handles lateral correction.
80. User noticed implicit autosteer and requested leaving it for now, with a
    deferred review when ready for deeper development. Added RALLY-13 describing
    the track-frame/vehicle-heading separation question. No broader physics
    redesign undertaken. Two-track demo tests still pass, including visible yaw.

81. RALLY-12 accepted: user reviewed the default demo and shared its silent
    20-second headless capture on Discord. Two-track host tests and native
    review cover centre-seeking control and visible steering. Temporary video
    remains outside the repository. Removed accepted demo review from TODO.
82. User authorized committing all remaining progress and pushing to the
    existing public bgates747/AgonArcade repository for migration to another
    development machine. Repository visibility remains public. Hardware frame
    pacing remains unresolved; RALLY-13 autosteer review remains deferred.
