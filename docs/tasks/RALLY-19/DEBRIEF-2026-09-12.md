# Clean checkpoint requested by the Author

R19-11 and R19-12 remain open. The Author requested a debrief and stopping point.
The running full-oval lifecycle test finished successfully. Stop development and
emulator testing here for discussion; do not treat the goal as complete.

Measured R19-10 sustained unpaced throughput: oval30.2042 FPS, Fuji30.1281 FPS.
Median native swap intervals31.6682/31.6487ms; p9536.7422/36.7128ms. Reciprocal
median (~31.6FPS) is not sustained average FPS. Normal gameplay is separately
paced30Hz. Scene traffic100bytes/frame, down88.5305/86.6944%; conservative eZ80
scene-construction work down91.9657/91.0111%. These CPU percentages exclude
physics, MOS driver work and UART waits. Real-UART timing is a separate test.
Hardware performance is untested. See PERFORMANCE.md and its raw qualification.

Double buffering does not inherently imply30Hz. The pinned native USERSPACE
path executes Canvas::swapBuffers synchronously, consumes one boolean vblank
signal inside VGABaseController::swapBuffers, then consumes another in
primitivesExecutionWait. Sources beneath the pinned Fab1.2.4 runtime:
- src/vdp/userspace-vdp-gl/src/canvas.cpp:647
- src/vdp/userspace-vdp-gl/src/displaycontroller.cpp:540,636,466
- src/vdp/userspace-vdp-gl/src/dispdrivers/vgabasecontroller.cpp:812
- src/vdp/rust_glue.cpp:35 sets the boolean signal
- src/main.rs:423 signals once per display iteration; its pacing uses5ms sleeps.
This is a specific explanation for near-half-refresh cadence, not a general
hardware rule. No controlled removal of either wait has been tested;60FPS is
not demonstrated. A minimal swap-only test and isolated second-wait experiment
would be the next investigation after discussion, without editing official files.

Both deterministic long replays passed18064 frames/602.133seconds each. Every
admitted80-byte state matched the sanitized host replay; no missing/duplicate
sequence, protocol error or nonfinite value. Independent audit:
evidence/golem-stability/replay-audit-inputs/qualification.json.
Coverage:59 natural lap crossings, all surfaces, both steering/lateral limits,
speed0..300, grip25..200,4659 nearby-visible-depth poses. Oval memory stayed at
3698 tracked allocations/720553bytes. Fuji added one16-byte metadata node at
frame8, then stayed at3772/843427. Both cleaned down to26/174986. These are native
heap_caps observations, not physical hardware heap or all host malloc paths.

Full-oval lifecycle passed210 events,27 accepted states, range/duplicate/gap/cut
rejection and recovery,65534-to0 sequence wrap, geometry/history preservation
on rejection, and three teardown/reload cycles. All836 owned IDs disappeared
using two distinct missing-copy tags; a foreign canary survived; equivalent
allocation phases matched. See evidence/golem-lifecycle/full-oval/results.json.
Fuji lifecycle remains unrun.

The unchanged compiler passed all13 sanitizer/golden/negative targets; Golem
commit934da5d records it. The checked loader passed738 ASan/UBSan fault cases.
It now validates generated asset size/FNV signatures before execution and holds
the full verified cleanup stream before transmission. FNV detects damage, not
malicious replacement. Native missing-bootstrap testing found37 retained art/
matrix buffers; the initialization-failure path now clears them, and the repeated
native test passes. Other native missing/corrupt variants remain pending.

Latest normal build: .work/frontend/checked-cleanup/bin/rally.bin (149068bytes),
SHA256 aac631bc1bbf8f694c0dc94fba0413c7ac4eab1d64f7b503ea5ee3ebbb3989e1.
Resident .vdp/.clr/.road assets remain identical to R19-09/R19-10. Strict
frontend_bridge.py verifies unchanged recurring prepare/submit, original headers
and resident assets; the only frontend body change is failure cleanup.
A final timing build exists at .work/render-work/checked-cleanup but has not run.
New integrity checks change startup work; final startup cost remains unmeasured.

Pending: Fuji lifecycle; other native loader-failure cases; final normal control/
exit regression; final timing/startup; closing audit and R19-12 delivery docs.
Earlier186 scene-image comparisons passed on unchanged resident scene code,
with exact vehicle pixels and frozen regional road/background thresholds.
No GUI alerts, hardware deployment, pushes or upstream firmware edits occurred.
