Oracle captures and oracle bridge succeeded. Candidate compiled Golem but its C++ diagnostic loader failed because memcpy lacked <string.h>. No candidate native images. Fixed in the separately named raw-trace-include-oval run.
